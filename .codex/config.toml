# Equivalent to Claude Code's bypassPermissions mode for this trusted project.
approval_policy = "never"
sandbox_mode = "danger-full-access"
model_reasoning_effort = "medium"

[features]
hooks = true
